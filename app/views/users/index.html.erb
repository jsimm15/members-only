<%= render "signedin", user: @user, post: @post %>

<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%=link_to user.username, user_path(user) %>
    </li>
  <% end %>

  <br><br>
  <%= link_to "New User", new_user_path %>
  <br>
  <%= link_to "Show All Posts", posts_path %>

</ul>